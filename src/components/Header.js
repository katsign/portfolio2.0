import Icon from '../assets/images/favicon.ico';

const Header = () => {
  return (
    <div>
      <img
        width="44"
        src={Icon}
        alt="Logo"
        id="logo"
      />
    </div>
  );
};

export default Header;
